spec:
  inputs:
    JOB_STAGE:
      default: lint
      description: "Stage for the central linter job"
    IMAGE:
      default: quay.io/aipcc-cicd/central-linter:v0.1.0
      description: "Container image to use for linting"
---
linter-central:
  stage: $[[ inputs.JOB_STAGE ]]
  image: $[[ inputs.IMAGE ]]
  rules:
    - if: $CI_COMMIT_MESSAGE =~ /^Draft:/
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
  script:
    - make -f $HOME/Makefile linter-central
  tags:
    - aipcc-k8s-cicd
